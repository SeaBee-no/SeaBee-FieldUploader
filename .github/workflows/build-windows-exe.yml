name: Build Windows EXE

on:
  workflow_dispatch: {}
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        shell: powershell
        run: |
          python -m pip install --upgrade pip
          pip install -r "app/requirements.txt"
          pip install pyinstaller

      - name: Build EXE with PyInstaller (onefile)
        shell: powershell
        run: |
          pyinstaller --noconfirm --clean --windowed --onefile `
            --name "SeaBee-FieldUploader" `
            "app/__main__.py"

      - name: Create release folder
        shell: powershell
        run: |
          New-Item -ItemType Directory -Force -Path "release" | Out-Null
          New-Item -ItemType Directory -Force -Path "release/resources" | Out-Null

          Copy-Item "dist/SeaBee-FieldUploader.exe" "release/SeaBee-FieldUploader.exe"

          Copy-Item "resources/rclone.conf.template" "release/resources/rclone.conf.template"
          Copy-Item "resources/defaults.txt" "release/resources/defaults.txt"
          Copy-Item "resources/bucket.conf.template" "release/resources/bucket.conf.template"
          Copy-Item "app/seabee.ico" "release/resources/seabee.ico"

      - name: Upload artifact (single zip download)
        uses: actions/upload-artifact@v4
        with:
          name: SeaBee-FieldUploader
          path: release/*
